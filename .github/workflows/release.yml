name: Release

on:
  release:
    types: [created]

jobs:
  build:
    name: Build Multi-Platform Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.24'

      - name: Build kubectl-mtv-mcp multi-platform binaries and archives
        run: make dist-all
        env:
          VERSION: ${{ github.event.release.tag_name }}

      - name: Upload kubectl-mtv-mcp release assets
        uses: softprops/action-gh-release@da05d552573ad5aba039eaac05058a918a7bf631
        with:
          files: |
            dist/kubectl-mtv-mcp-${{ github.event.release.tag_name }}-linux-amd64.tar.gz
            dist/kubectl-mtv-mcp-${{ github.event.release.tag_name }}-linux-amd64.tar.gz.sha256sum
            dist/kubectl-mtv-mcp-${{ github.event.release.tag_name }}-linux-arm64.tar.gz
            dist/kubectl-mtv-mcp-${{ github.event.release.tag_name }}-linux-arm64.tar.gz.sha256sum
            dist/kubectl-mtv-mcp-${{ github.event.release.tag_name }}-darwin-amd64.tar.gz
            dist/kubectl-mtv-mcp-${{ github.event.release.tag_name }}-darwin-amd64.tar.gz.sha256sum
            dist/kubectl-mtv-mcp-${{ github.event.release.tag_name }}-darwin-arm64.tar.gz
            dist/kubectl-mtv-mcp-${{ github.event.release.tag_name }}-darwin-arm64.tar.gz.sha256sum
            dist/kubectl-mtv-mcp-${{ github.event.release.tag_name }}-windows-amd64.zip
            dist/kubectl-mtv-mcp-${{ github.event.release.tag_name }}-windows-amd64.zip.sha256sum

      - name: Wait for assets to be available
        run: |
          echo "Waiting 30 seconds for GitHub to propagate release assets..."
          sleep 30

      - name: Verify all assets are available
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          ASSETS=(
            "kubectl-mtv-mcp-${VERSION}-linux-amd64.tar.gz"
            "kubectl-mtv-mcp-${VERSION}-linux-arm64.tar.gz" 
            "kubectl-mtv-mcp-${VERSION}-darwin-amd64.tar.gz"
            "kubectl-mtv-mcp-${VERSION}-darwin-arm64.tar.gz"
            "kubectl-mtv-mcp-${VERSION}-windows-amd64.zip"
          )
          
          echo "Verifying release assets are downloadable..."
          for asset in "${ASSETS[@]}"; do
            url="https://github.com/${{ github.repository }}/releases/download/${VERSION}/${asset}"
            echo "Checking: $url"
            if curl -sSL --fail -I "$url" > /dev/null; then
              echo "✅ $asset is available"
            else
              echo "❌ $asset is not available"
              exit 1
            fi
          done
          echo "All assets verified successfully!"
